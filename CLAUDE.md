@AGENTS.md

## Development Rules

- After every code change, run `npm run build` to confirm there are no errors before saying the task is done.
- Never confirm completion without a clean build output.
